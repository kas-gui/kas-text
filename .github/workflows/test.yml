name: Tests

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  nightly:
    name: Nightly
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true
          components: rustfmt, clippy
      - name: Rustfmt check
        uses: actions-rs/cargo@v1
        with:
          command: fmt
          args: --all -- --check
      - name: doc
        run: RUSTDOCFLAGS="--cfg doc_cfg" cargo doc --features markdown --no-deps
      - name: Test Harfbuzz
        run: cargo test --features harfbuzz
      - name: Clippy
        run: cargo +nightly clippy --features shaping,markdown,raster,serde
      - name: test (gat)
        run: cargo test --all-features

  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]
        toolchain: [stable]
        include:
          - os: ubuntu-latest
            toolchain: "1.58.0"
          - os: ubuntu-latest
            toolchain: beta

    steps:
      - uses: actions/checkout@v2
      - name: Install toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: ${{ matrix.toolchain }}
          override: true
      - name: test
        run: cargo test --features markdown,shaping,raster,serde
